const storageDirectory = './src/storage/';

const storageFiles = {
    CATEGORIES: 'categories.json',
    ORDERS: 'orders.json',
    PRODUCTS: 'products.json',
    USERS: 'users.json',
};

module.exports = { storageDirectory, storageFiles };
